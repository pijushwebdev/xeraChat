import { createSlice } from "@reduxjs/toolkit";


const initialState = {};

const conversationsSlice = createSlice({
    name: 'conversations',
    initialState,
    reducers: {}
})


export const {} = conversationsSlice.reducer;

export default conversationsSlice.actions;